--- /usr/share/lipstick-jolla-home-qt5/lockscreen/Clock.qml	2017-04-04 21:18:44.370551438 +0200
+++ /usr/share/lipstick-jolla-home-qt5/lockscreen/Clock.qml	2017-04-04 21:22:42.194097914 +0200
@@ -32,34 +32,48 @@
             bottomMargin: -timeText.font.pixelSize
             horizontalCenter: parent.horizontalCenter
         }
-        font { pixelSize: Theme.fontSizeHuge * 2.0; family: Theme.fontFamilyHeading }
+        font { pixelSize: Theme.fontSizeHuge * 3.5; family: Theme.fontFamilyHeading }
+        text: Qt.formatTime(new Date(), "hh")
     }
+    ClockItem {
+        id: timeTextMin
+        color: Theme.primaryColor
+        // Ascender of the time to the top of the clock.
+        anchors {
+            top: timeText.baseline
+            topMargin: -Theme.paddingLarge
+            bottom: parent.top
+            bottomMargin: -timeText.font.pixelSize
+            horizontalCenter: parent.horizontalCenter
+        }
+        font { pixelSize: Theme.fontSizeHuge * 3.5; family: Theme.fontFamilyHeading }
+        text: Qt.formatTime(new Date(), "mm")
+    }
+
+
+    Timer {
+        id: timerClock
+        interval: 60; running: true; repeat: true
+	onTriggered: {
+        	timeText.text = Qt.formatTime(new Date(), "hh")
+        	timeTextMin.text = Qt.formatTime(new Date(), "mm")   
+    	}
+     }
 
     Text {
         id: weekday
         anchors {
             horizontalCenter: parent.horizontalCenter
-            top: timeText.baseline
+            top: timeTextMin.baseline
             topMargin: Theme.paddingMedium
         }
         color: timeText.color
         font { pixelSize: Theme.fontSizeLarge; family: Theme.fontFamily }
         text: {
             var day = Format.formatDate(time, Format.WeekdayNameStandalone)
-            return day[0].toUpperCase() + day.substring(1)
+            var month = Format.formatDate(time, Format.DateMediumWithoutYear)
+            return day[0].toUpperCase() + day.substring(1, 3) + ", " + month
         }
     }
 
-    Text {
-        id: month
-        anchors {
-            horizontalCenter: parent.horizontalCenter
-            top: weekday.baseline
-            topMargin: Theme.paddingMedium
-        }
-        color: timeText.color
-        font { pixelSize: Theme.fontSizeExtraLarge * 1.1; family: Theme.fontFamily }
-
-        text: Format.formatDate(time, Format.DateMediumWithoutYear)
-    }
 }
